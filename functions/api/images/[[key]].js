export async function onRequestGet({env,params}){if(!env.CABIN_IMAGES)return new Response('Storage unavailable',{status:503});const key=Array.isArray(params.key)?params.key.join('/'):params.key;const obj=await env.CABIN_IMAGES.get(key);if(!obj)return new Response('Not found',{status:404});return new Response(obj.body,{headers:{'content-type':obj.httpMetadata?.contentType||'image/jpeg','cache-control':'public, max-age=31536000, immutable'}});}
