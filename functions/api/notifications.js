import { json, currentStaff, manage } from '../_lib/auth.js';
export async function onRequestGet({env,request}) { const staff=await currentStaff(request,env);if(!manage(staff))return json({error:'Permisos insuficientes'},403);try{const r=await env.DB.prepare('SELECT * FROM notifications ORDER BY created_at DESC LIMIT 100').all();return json({notifications:r.results})}catch{return json({notifications:[]})} }
export async function onRequestPatch({env,request}) { const staff=await currentStaff(request,env);if(!manage(staff))return json({error:'Permisos insuficientes'},403);try{await env.DB.prepare('UPDATE notifications SET is_read=1').run()}catch{}return json({ok:true}); }
